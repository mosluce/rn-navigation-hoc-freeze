import React from 'react';
import {
  View,
  Button,
  Text,
} from "react-native";

const DD = ({ navigation }) => (
  <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
    <Text>DD</Text>
  </View>
)

export default DD;